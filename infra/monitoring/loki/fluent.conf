<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type loki
  url "http://loki:3100"
  <label>
    container_name $["container_name"]
    swarm_service $["com.docker.swarm.service.name"]
  </label>
  <buffer>
    flush_interval 10s
  </buffer>
</match>